# Serverless Image Pipeline. How to edit this file: docs/guides/case-studies.md
slug: serverless-image-pipeline
title: Serverless Image Pipeline
summary: Upload through API Gateway, process in Lambda and Step Functions, rebuild it all from CloudFormation.
group: backend
tags: [AWS Lambda, S3, Step Functions, CloudFormation]
live:
code: https://github.com/patel-kshitij/Serverless-Image-Processing
facts: []
architecture:
  # Drafted from the project's sentence and tools; check against the real code (STATUS entry 28).
  boxes:
    - { id: client, label: Client, col: 0, row: top }
    - { id: api, label: API Gateway, col: 1, row: top }
    - { id: lambda, label: Lambda, col: 2, row: top }
    - { id: sfn, label: Step Functions, col: 3, row: top }
    - { id: s3, label: S3, col: 3, row: bottom }
    - { id: cfn, label: CloudFormation, col: 1, row: bottom }
  arrows:
    - { from: client, to: api, step: 1 }
    - { from: api, to: lambda, step: 2 }
    - { from: lambda, to: sfn, step: 3 }
    - { from: sfn, to: s3, step: 4 }
    - { from: cfn, to: s3, step: 5 }
